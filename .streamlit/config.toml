# Disable auto-discovery of ui/pages/*.py as sidebar nav entries.
# Page routing is handled by if/elif dispatch in ui/app.py instead.
[client]
showSidebarNavigation = false

# Reduce WebSocket disconnects in Docker (Change 3A).
[server]
enableCORS = false
enableXsrfProtection = false
